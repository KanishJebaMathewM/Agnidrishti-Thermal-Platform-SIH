node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Personal Claude Code overrides (may contain secrets, e.g. MCP server API keys)
.claude/settings.local.json

# Personal Claude Code tooling (design skill, MCP config) — not part of the
# application; not shared with the team via this repo
.claude/skills/
.mcp.json

# Python (ml/, workers/, backend/)
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Trained ML artifacts (regenerate via ml/training/*) and generated reports
ml/models/*.joblib
ml/models/*.json
ml/evaluation/reports/*.json
ml/evaluation/reports/*.png
ml/evaluation/reports/*.txt
